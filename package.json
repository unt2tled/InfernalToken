{
  "name": "infernaltoken",
  "version": "1.0.0",
  "description": "The official internal token of Hell",
  "license": "ISC",
  "author": "Mark Kadash",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:test": "npx hardhat ignition deploy ./ignition/modules/Token.js --network sepolia",
    "test": "npx hardhat test",
    "prettier": "npx prettier --write --plugin=prettier-plugin-solidity 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "hardhat": "^2.24.1",
    "prettier": "^3.5.3",
    "prettier-plugin-solidity": "^2.0.0"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0"
  }
}
